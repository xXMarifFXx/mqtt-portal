{
  "name": "mqtt-portal",
  "version": "0.5.0",
  "description": "Class-code self-registration + admin panel for a Mosquitto broker (Dynamic Security).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "DYNSEC_MODE=mock COOKIE_SECURE=false node server.js",
    "hashpw": "node scripts/hashpw.js",
    "privacy:expired": "node scripts/purge-expired.js",
    "test": "node test/test_validate.js && node test/test_dynsec.js && node test/test_config.js && node test/test_csrf.js && node test/test_session_store.js && node test/test_mosquitto_config.js && node test/test_privacy.js && node test/test_registration_limits.js && node test/test_http_security.js",
    "test:recovery": "bash test/test_recovery.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "express-session": "^1.18.0",
    "helmet": "^7.1.0",
    "mqtt": "^5.10.1",
    "session-file-store": "^1.5.0"
  }
}
